#include "DecToBin.h"
#include "DecToHex.h"
using namespace std;

int main(){
    // 调试 DecToBin 的源码
    // DecToBin num;

    // num.getDec();

    // num.toBin();
    // num.toDec();
    // num.printData();

    // 调试 DecToHex 的源码
    DecToHex h;
    h.getHex();
    
    h.toHex();
    h.toDec();
    
    h.printData();
    return 0;
}